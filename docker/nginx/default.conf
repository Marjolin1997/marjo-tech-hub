server {
    listen 80;
    server_name _;
    root /var/www/html/public;
    index index.php index.html;

    # Network Diagnostics sends a bounded upload payload (currently 4 MB;
    # backend rejects payloads above 10 MB). Route this endpoint directly
    # to Laravel so an internal redirect cannot fall back to Nginx's
    # default 1 MB body limit.
    location = /api/network-test/upload {
        client_max_body_size 10m;
        include fastcgi_params;
        fastcgi_pass app:9000;
        fastcgi_param SCRIPT_FILENAME $document_root/index.php;
        fastcgi_param SCRIPT_NAME /index.php;
    }

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        include fastcgi_params;
        fastcgi_pass app:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param PATH_INFO $fastcgi_path_info;
    }

    location ~ /\.ht {
        deny all;
    }
}
